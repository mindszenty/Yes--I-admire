<%= form_for([@article, @article.comments.new], :html => {:style => 'display: none;'}) do |f| %>
    <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
    </div>
    <div class="actions">
    <%= f.submit 'Add' %>
    </div>
<% end %>
